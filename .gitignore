# Environment files — never commit
.env
.env.local
backend/.env
frontend/.env.local

# Python
backend/venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Quran data — large file, regenerated on first run
backend/quran/quran_data.json

# HuggingFace model cache
.cache/huggingface/

# Node
frontend/node_modules/
frontend/.next/

# OS
.DS_Store
Thumbs.db

# Agent / IDE caches
.claude/

# Scaffold files (create-next-app leftovers)
frontend/public/file.svg
frontend/public/globe.svg
frontend/public/next.svg
frontend/public/vercel.svg
frontend/public/window.svg

backend/test.mp3

# Test audio — real voice recordings + downloaded recitations; never commit (privacy + size).
# The 5 original studio clips (108001.mp3 etc.) are fine to commit; all new additions are excluded.
backend/tests/data/fatiha.m4a
backend/tests/data/wrong recite.m4a
backend/tests/data/recite_*.mp3
backend/tests/data/recite_*.m4a
